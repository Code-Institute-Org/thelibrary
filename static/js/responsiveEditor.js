/**
 * Adjusts width of installed ckeditor rich text editor
 * after it has loaded in the DOM, to make it responsive.
 */
setTimeout(function(){
    document.getElementById('cke_id_body').style.width = '100%';
}, 500);